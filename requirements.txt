# --- Serving (required for the deployed API to run) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
joblib>=1.3.0
numpy>=1.24.0
scikit-learn>=1.3.0
bcrypt>=4.1.0
groq>=0.11.0
a2wsgi>=1.16.0

# --- Model training only (needed if you re-run the training
# notebook/script, NOT needed to serve /predict on PythonAnywhere) ---
pandas>=2.0.0
openpyxl>=3.1.0
matplotlib>=3.7.0
seaborn>=0.12.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0